# Roadmap

Written properly in M2. Placeholders for what is already known to be owed:

- Prove the annotations against a real Tk window read from a separate process.
- Wire the Tk 9.1 native path (TIP 733) instead of deferring to it.
- Verify announcements with NVDA rather than stopping at the accessibility tree.
- CI, and publishing.
