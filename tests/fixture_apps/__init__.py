"""The applications the gui specs launch, importable so the words match.

A package rather than a bare directory so a spec can import the strings the
application puts on screen instead of retyping them. A gui spec that asserts on
its own copy of a caption passes the day somebody changes the app's.
"""
